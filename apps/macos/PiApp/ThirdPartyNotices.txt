Bello Agent third-party notices

Bello Agent links one third-party component: Sparkle 2.8.1
(https://sparkle-project.org), the update framework, which ships its own
LICENSE inside Sparkle.framework in the application bundle. The terminal
emulator, the conversation page, Markdown rendering and syntax colouring are
the application's own code.
